name: SFDX Test Run on Push

on: [push]

jobs:
  test:
  
    runs-on: ubuntu-latest
    
    steps:
      - uses: sfdx-actions/setup-sfdx@v1
        with:
          sfdx-auth-url: https://resilient-shark-47mjo0-dev-ed.lightning.force.com/
      - name: sfdx-test-run
        run: sfdx force:source:push --targetusername gael.cangy@resilient-shark-47mjo0.com
        run: sfdx force:org:list