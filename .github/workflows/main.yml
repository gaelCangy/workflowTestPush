name: SFDX Test Run on Push

on: [push]

jobs:
  test:
  
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@master

      - uses: sfdx-actions/setup-sfdx@v1
        with:
          sfdx-auth-url: force://$CLIENTIDRESILIENTSHARK::5Aep861wfm9ssIBS_.fn6UOXlmEa4gCMxAkaT1KAWW5Dnk19N9tNBqdhKZS7MIDXIzCenttYrhlBLnsIRVn9bNy@resilient-shark-47mjo0-dev-ed.my.salesforce.com

      - name: sfdx push to org
        run: |
          ls -a
          sfdx force:org:list
          sfdx force:source:deploy -x manifest/package.xml
          